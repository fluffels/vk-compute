#version 450

layout(local_size_x=32) in;

layout(set=0, binding=0) buffer OutputBuffer {
    uint gIDs[];
} outputData;

void main() {
    uint gID = gl_GlobalInvocationID.x;
    outputData.gIDs[gID] = gID;
}
