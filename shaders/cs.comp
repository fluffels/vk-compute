#version 450

layout(local_size_x=1, local_size_y=1) in;

layout(set=0, binding=0) buffer OutputBuffer {
    uint gIDs[];
} outputData;

void main() {
    // TODO: set width as a uniform
    uint idx = gl_GlobalInvocationID.x + gl_GlobalInvocationID.y * 1920;
    outputData.gIDs[idx] = 0x00ffff00;
}
