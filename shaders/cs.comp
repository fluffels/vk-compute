#version 450

layout(local_size_x=32, local_size_y=32) in;

layout(set=0, binding=0) buffer OutputBuffer {
    uint gIDs[];
} outputData;

void main() {
    uint idx = gl_GlobalInvocationID.x + gl_GlobalInvocationID.y * 32;
    outputData.gIDs[idx] = 0xffffffff;
    outputData.gIDs[0] = 0xffffffff;
}
